"""
Matrix

"""
def searchMatrix(matrix: list[list[int]], target: int) -> bool:
        if not matrix: return -1

        rows, cols = len(matrix), len(matrix[0])


# Example usage
if __name__ == "__main__":
    # TODO: Add example calls to searchMatrix
    print(searchMatrix([]))
