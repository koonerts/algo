

def basic_knapsack(profits, weights, capacity) -> int:

    def recurse(curr_profit, curr_weight):
        pass

    max_profit = 0
    curr_profit =
    for i in range(len(profits)):
        max_profit = max()